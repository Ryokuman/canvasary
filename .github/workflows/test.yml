name: CI

on:
  push:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: ls -al
      - uses: actions/checkout@v4
      - run: ls -al

      - name: test
        run: sh /home/ubuntu/canvasary/deploy.sh
