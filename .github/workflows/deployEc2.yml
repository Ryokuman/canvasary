name: CI

on:
  push:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: ls -al

      # - name: test2
      #   run: sh /home/ubuntu/canvasary/deploy.sh
